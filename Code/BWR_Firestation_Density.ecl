#option('obfuscateOutput', true)

IMPORT $;

OUTPUT($.firestation_Density.file);
// newrecord := record
//     $.firestation_density.file.state;
//     $.firestation_density.file.num_firestations;    
//     newdensity := $.firestation_density.file.density

// end;
// newtable := table()